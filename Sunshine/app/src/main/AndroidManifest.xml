<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.sunshine.app" >

    <!-- Special permissions for the Sunshine app -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- end -->
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <!--  e.g. within the main activity you can hack the orientation to NOT refresh using:
                android:configChanges = "orientation|keyboardHidden|screenSize"
          -->
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DetailActivity"
            android:label="@string/title_activity_detail"
            android:parentActivityName=".MainActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="MainActivity" />
        </activity>
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings"
            android:parentActivityName=".MainActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="MainActivity" />
        </activity>

        <!--
                registering the WeatherProvider (ContentProvider) in the manifest
          -->

        <provider
            android:authorities="com.example.android.sunshine.app"
            android:name=".data.WeatherProvider"
            android:enabled="true"
            android:exported="true"
            android:permission="com.myapp.BLAH"
         >
         </provider>

    </application>

    <permission
        android:name="come.myapp.BLAH"
        android:protectionLevel="dangerous"
        android:label="Licenced to BLAH"
    >
    </permission>

</manifest>
